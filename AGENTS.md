# 项目工作约定

## 目标与上下文

- 默认用中文沟通。优先级：Agent / harness engineering > 后端开发 > 后训练（SFT / agentic RL）。
- 2026 年 11 月中旬前准备 Agent 开发实习项目；2027 年 4 月前补齐后训练成果。按每周 30–40 小时估算，提供阶段安排，避免过细排期。
- 首次进入项目先读 `docs/PLAN_REVIEW_2026-09-19.md`；按任务查阅 `docs/` 中的对话和算力指南。用户最新明确要求优先于历史文档；历史模型结论须核实。
- 当前路线：HolmesGPT 工程基座 + 原生评测 + SREGym-Lite diagnosis 外部评测。源码层可行不等于已跑通；实际部署和评测状态以运行记录为准。
- `.research/` 是调研证据与源码快照，不等同于项目实现。详细计划、实验结果和进展写入 `docs/`，本文件只保留稳定约定。

## 开发原则

- 先读相关源码和已有能力，再围绕真实需求修改；复用上游机制，避免大规模重写、重复造平台和为简历强行加功能。
- Harness 不限一个方向：依据源码、trace 和失败案例提出假设，分别对照、组合与消融，以证据决定保留哪些改动。
- 后端主线可包含原生组件优化和必要的新功能（如实验 / rollout runner、可靠轨迹导出）。Issue 是灵感来源，merged PR 是背书，不代替系统设计叙事。
- 后端工作应能解释：系统边界、并发选择、数据建模、失败与幂等重试及一致性、可观测性、瓶颈定位。围绕实际实现回答，避免为覆盖题目堆组件。
- 无法证明价值时撤回本次不必要的复杂度；深入理解、复现、集成和验证原生组件是有效回退。区分上游能力与本人贡献，不编造收益、指标或 PR 状态。
- 在已授权范围内持续推进可逆工作，不反复确认；保留用户现有改动，不擅自覆盖或回滚。
- 实施前将当轮确认计划存入 `docs/plans/`，结果另写 `docs/reports/`。当前运行入口和证据位置见 `README.md`；pci-2 上所有集群操作必须显式使用 `sre-agent-dev` 的专用 kubeconfig，禁止误用现有 k3s。

## 验证与数据

- 修改前查阅目标仓库的局部约定和真实构建 / 测试入口，不假定当前调研目录已有可运行工程。
- 做与改动相称的验证：正确性、可靠性、性能或任务效果均可构成价值；区分已测试、仅源码推断和尚未验证。
- 对照评测固定或记录模型、judge、预算、工具权限、代码版本和记忆快照；区分 Agent 失败、环境失败与评判失败，报告样本量、成本和局限。
- 从第一天保留后训练兼容轨迹：优先复用 SREGym ATIF 与 SQLite 索引 / 导出；记录每次实际模型输入（含压缩和检索结果）、工具定义及调用 ID / 结果、成本、来源和版本。
- 按故障族隔离训练、开发和测试数据；防止案例 ID、标准答案、skills 或记忆泄漏。先贯通数据与评测管线，再实现完整 RL。

## 资源与敏感信息

- 保底资源为两台独立 RTX 4090 24GB 节点；共享 48GB 4090 集群按 Slurm 规则最多并发两卡，可能排队。A800 暂不纳入保证资源；详细限额见算力指南。
- 独立节点显存不可直接相加。共享 GPU 使用遵循集群指南，部署前核实目标节点和当前状态。
- API key、SSH 密码等仅在任务需要时读取；不得写入代码、文档、trace、日志或提交记录。示例使用占位符，轨迹导出须脱敏。
